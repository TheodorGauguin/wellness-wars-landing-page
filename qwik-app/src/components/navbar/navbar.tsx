import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="navbar bg-base-200">
      <a class="btn btn-ghost normal-case text-xl">Wellness Wars</a>
    </div>
  );
});